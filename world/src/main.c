#include <magic.h>
#include <magic2.h>
#include <stdio.h>

int main(){
    printf("The first magic number is %d\n", MAGIC_NUMBER);
    printf("The second magic number is %d\n", MAGIC_NUMBER_2);
    return 0;
}
